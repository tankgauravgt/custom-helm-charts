apiVersion: v1
kind: Service
metadata:
  name: dozerdb-headless
  namespace: dozerdb-ns
spec:
  clusterIP: None
  selector:
    app: dozerdb
  ports:
    - port: 7474
      name: https
    - port: 7687
      name: bolt