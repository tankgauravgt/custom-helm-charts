apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dozerdb-cert
  namespace: dozerdb-ns
spec:
  secretName: dozerdb-tls
  dnsNames:
    - {{ "*.example.com" }}
  issuerRef:
    name: letsencrypt-prod
    kind: Issuer